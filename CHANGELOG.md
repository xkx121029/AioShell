# Changelog

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 规范，版本号遵循 [Semantic Versioning](https://semver.org/lang/zh-CN/)。

## [1.7.0] - 2026-08-31

### 新增：多会话批量管理
- 会话列表顶栏「批量管理」，多选后支持批量导出 / 归档 / 删除，事务保证一致性

### 新增：Markdown 增强编辑
- 渲染层补齐表格、任务列表（- [x]）、有序列表；编写侧新增 Markdown 辅助编辑工具

### 新增：对话置顶与分组
- 单个会话可置顶（固定在顶部）；列表按「置顶 / 今天 / 昨天 / 更早」分组展示

### 新增：夜间深色模式细化
- 深色模式下可选 AMOLED 纯黑（背景/表面切换为纯黑，OLED 更省电更纯粹）

### 新增：对话归档
- 归档后从主列表隐藏，进入归档页可恢复或彻底删除；数据库迁移 V6

### 新增：自动生成标题
- 新对话首条消息完成后自动生成标题；生成失败回退本地规则（提取首行正文）

### 新增：输入草稿自动保存
- 输入内容按会话维度防抖持久化，退出后重进自动还原，发送/清空后置空

### 新增：数据备份与恢复
- 全量导出（接口配置 + 会话 + 消息 + 草稿 + 置顶/归档状态）为单个 JSON 经系统分享保存
- 从备份文件恢复，按 id 覆盖写入、可重复导入，接口 API Key 恢复时重新加密

### 新增：AI 消息富文本渲染
- Markdown 表格 / 任务列表 / 有序列表等富文本结构在 AI 消息中完整渲染

### 新增：会话统计页
- 统计会话数 / 消息数 / 用户与 AI 消息数 / 总字符数 / 累计 Token 估算

### 新增：多接口快速切换
- 会话列表顶栏配置入口弹出快速下拉，无需进入设置页即可切换当前接口档案

### 新增：界面自定义
- 自选强调色（6 种预设，含恢复默认）
- 聊天正文字号（13–20sp）与行距（0–10sp）独立调节，即时生效

### 构建
- 版本提升至 1.7.0（versionCode 9），数据库版本升至 V6

## [1.5.0] - 2026-08-31

### 新增：对话全文搜索
- 会话列表顶栏新增搜索入口，支持跨所有会话按关键词搜索消息正文
- 输入防抖（300ms）触发，自动过滤错误消息，按时间倒序展示
- 点击搜索结果自动定位到对应会话并滚动高亮目标消息，支持一键跳转
- 结果卡片展示角色标识、会话标题、时间与命中摘要

### 新增：提示词模板库
- 顶栏「模板」入口进入模板库，内置写作 / 翻译 / 代码 / 总结 / 头脑风暴五大类共 12 个预设模板
- 支持自定义新增 / 编辑 / 删除模板（内置模板仅可编辑不可删除）
- 模板内容支持 `{{text}}` 占位符，点击「使用」返回对话页并预填输入框
- 数据库迁移 V4：新增 prompt_template 表

### 新增：对话导出 / 分享
- 对话页顶栏新增导出入口，支持 **Markdown / 纯文本 / JSON** 三种格式
- 导出的思考过程（reasoning）一并包含，含会话标题、角色、时间戳
- 通过系统分享面板分享（FileProvider 提供内部缓存文件 URI，免存储权限）

### 新增：TTS 语音朗读
- 长按 AI 回复消息 →「朗读」，使用系统 TTS 逐字朗读
- 再次触发可「停止朗读」，界面同步播放/停止状态
- 离开对话页自动停止，避免后台漏音

### 构建
- 版本提升至 1.5.0（versionCode 7），数据库版本升至 V4

## [1.4.0] - 2026-08-25

### 新增：杂项 AI（对话开场分析）
- **新对话首条消息**：先调用杂项 AI 对对话进行**命名 + 意图分析**，并要求返回**纯 JSON**
- 杂项 AI 依据意图**自动生成主模型应采用的"身份"与"回答风格"建议**
- 把上述身份/风格/意图组织成 **system 握手信息**，注入主模型第一条请求，使回复更贴合语境
- 未启用或未配置杂项 AI 时，自动回退使用当前聊天模型完成该开场分析
- system 注入仅限本次请求，不落库，避免污染历史上下文

## [1.3.0] - 2026-08-25

### 新增：本地语音输入
- 接入 Vosk 离线语音识别（本地推理，不联网）
- 语音模型应用内引导下载（默认中文小模型 vosk-model-small-cn-0.22，含常见英文），带下载进度
- **长按输入框即开启语音输入，并在输入框处呈现光场可视化**：输入框位置打亮一束**椭圆光源（由下至上渲染、颜色逐渐变淡）**；上方**三根竖向声波条随真实语音音量实时变化**；识别文字**在空中实时累积浮显**（带光晕）。支持"完成"填入输入框，上滑取消（取消预览时文字转为粒子漂浮）
- 录音权限（RECORD_AUDIO）运行时申请，识别结果完全本地
- 中英混合：中文小模型可识别常见英文

### 构建
- 版本提升至 1.3.0（versionCode 5），引入 com.alphacephei:vosk-android

## [1.2.2] - 2026-08-25

### 新增：代码块增强
- 自研轻量语法高亮（关键字 / 字符串 / 注释 / 数字），覆盖 Kotlin / Java / Python / JS / TS / JSON / Bash / SQL
- 代码块一键复制（复制反馈对勾，2s 恢复）
- 超过 20 行代码默认折叠，可展开全部
- 语言标签显示，超长代码横向滚动
- 高亮结果缓存，超长码块降级纯文本

### 新增：思考模式
- SSE 解析思考内容（兼容 `reasoning_content` / `reasoning` / `thinking` 字段）
- 思考过程流式展示、可折叠，思考耗时统计
- 模型级开关（配置档案），对话页顶栏快捷开关 + 配置页主开关双入口，状态一致
- 数据库迁移 V2：message 表新增 reasoning / reasoningDurationMs

### 新增：多模态输入
- 系统 Photo Picker 选图（免权限，上限 4 张）
- 图片压缩（最长边 1536 / JPEG / 质量 80）并存入私有目录
- OpenAI 兼容视觉协议构造（text + image_url base64）
- 输入框附件缩略图预览与移除、历史消息图片回显、点击全屏查看
- 数据库迁移 V3：新增 message_attachments 表

### 构建
- 版本号提升至 1.2.2（versionCode 4），数据库版本升至 V3

## [1.1.2] - 2026-08-25

### 修复
- 修复主题切换后输入草稿丢失（`rememberSaveable`）
- 修复长对话滚动卡顿与不自跟（智能跟随：仅停留在底部时自动滚动）
- 修复 Markdown 未闭合代码块在流式过程中的渲染错乱（超长内容可横向滚动）
- 修复配置页表单校验提示缺失（校验集中到 ViewModel，字段级错误提示）
- 修复键盘遮挡输入框（`imePadding` 适配）
- 清理深浅色取色残留，统一走语义 Token，避免切换闪烁

### 优化
- 输入框多行自适应（高度随内容增长，超限内部滚动）
- 新增"回到底部"悬浮按钮（上翻历史时出现）
- 会话列表摘要统一规则（代码降级为 [代码]、超长截断）
- 关键操作触觉反馈（发送 / 复制 / 长按）
- 无障碍：流式生成动态状态 `liveRegion`、语义化内容描述

### 性能
- 流式输出智能跟随，历史消息稳定重组

## [1.1.0] - 2026-08-25

### UI / UX 专项优化（无核心业务功能变更）

- **设计系统（Design Token）**
  - 新增语义化颜色 Token（`AppColorScheme`）与双主题映射（浅色 / 深色）。
  - 新增间距（4dp 网格）、圆角、动效时长 Token。
- **主题系统**
  - 支持浅色 / 深色 / 跟随系统三种模式，选择持久化（DataStore）。
  - 会话页顶栏主题切换入口。
- **基础组件库**
  - 抽取 `AppButton` / `AppTextField` / `AppCard` / `EmptyState` / `LoadingState` / `ErrorState` / `AppDialog` / `MessageBubble` 统一组件。
- **Markdown 渲染**
  - 自研轻量解析器，支持标题 / 列表 / 引用 / 粗体 / 斜体 / 行内代码 / 代码块 / 分隔线。
  - 流式过程中容忍未闭合语法。
- **动效**
  - 消息出现淡入上移、流式光标呼吸动画。
- **页面重构**
  - 会话列表：会话摘要预览、空态区分、统一卡片。
  - 对话页：Markdown 气泡、流式光标、长按菜单（复制 / 重新生成）。
  - 配置页：表单分组、参数滑块（temperature / top_p / max_tokens）、分色连接反馈。
- **无障碍**
  - 图标按钮语义化 contentDescription、触控目标 ≥ 48dp。

### 构建

- 版本号提升至 1.1.0（versionCode 2）。

## [1.0.0] - 2026-08-24

### 新增

- **接口配置**
  - 自定义 Base URL、API Key、模型名。
  - 参数调节：temperature、max_tokens、top_p。
  - 连接测试（校验配置可用性）。
  - 多接口档案保存与切换。
- **会话管理**
  - 多会话创建 / 切换 / 重命名 / 删除。
  - 历史会话本地持久化（Room）。
- **对话交互**
  - 上下文记忆（携带历史消息）。
  - SSE 流式输出（打字机效果），支持中途停止。
  - 复制回复内容。
  - 中文错误提示。
- **数据与安全**
  - API Key 使用 Android Keystore（AES/GCM）加密存储。
  - 对话数据纯本地，无任何对外上传（除用户自配接口）。
  - 无广告、仅申请网络权限。
- **体验**
  - Material 3 Expressive 视觉 + 自研「海玻璃 / 深渊翡翠」双主题。
  - 跟随系统的深色模式。
  - 可切换的启动器应用图标（含用户自定义素材）。

### 构建

- 技术栈：Kotlin / Jetpack Compose / Hilt / Room / DataStore / OkHttp + Ktor SSE。
- minSdk 26，targetSdk 35。